---
name: "ci"

# yamllint disable-line rule:truthy
on:
  pull_request:
    branches:
      - "*"
  push:
    branches:
      - main

permissions:
  attestations: write
  id-token: write
  contents: write

jobs:
  strategy:
    name: strategy
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate matrix
        id: matrix
        run: |
          echo "matrix=$(make list | grep ^bin/ | jq -ncMR '[inputs | select(length>0)]')" >>"$GITHUB_OUTPUT"

  build:
    name: build.${{ matrix }}
    runs-on: ubuntu-24.04
    needs: strategy
    strategy:
      matrix: ${{fromJson(needs.strategy.outputs.matrix)}}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          make "${{ matrix }}"

      - name: List built artifacts
        run: |
          ls -l bin/
